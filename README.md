# Simple Weather App

##Prerequsitions

Python 3.7
Flask
Requests

## Env vars
to run in debug mode
$ set FLASK_ENV=development

To run Flask development server:

$ set FLASK_APP = app.py

## Local Development

to start flask app:
$ flask run